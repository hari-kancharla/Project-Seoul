diff --git a/chrome/browser/ui/views/location_bar/location_bar_view.cc b/chrome/browser/ui/views/location_bar/location_bar_view.cc
--- a/chrome/browser/ui/views/location_bar/location_bar_view.cc
+++ b/chrome/browser/ui/views/location_bar/location_bar_view.cc
@@ -370,59 +370,18 @@
   seoul_action_mode_label->SetVisible(false);
   seoul_action_mode_label_ = AddChildView(std::move(seoul_action_mode_label));
 
-  // Arc reaches its Boost editor from a paintbrush in the Site Control Panel
-  // at the right of the URL bar, which is the affordance that makes the
-  // feature discoverable at all - a command-palette-only entry point is not
-  // findable by anyone who does not already know the feature exists. This is
-  // Seoul's equivalent, in the same place, and it opens the same editor the
-  // command surface does.
-  auto seoul_boost_button = std::make_unique<views::ImageButton>(
-      base::BindRepeating(&LocationBarView::OnSeoulBoostButtonPressed,
+  // Group page tools behind one entry so the address remains readable.
+  auto controls = std::make_unique<views::ImageButton>(
+      base::BindRepeating(&LocationBarView::OnSeoulSiteControlsPressed,
                           base::Unretained(this)));
-  seoul_boost_button->SetImageHorizontalAlignment(
-      views::ImageButton::ALIGN_CENTER);
-  seoul_boost_button->SetImageVerticalAlignment(
-      views::ImageButton::ALIGN_MIDDLE);
-  seoul_boost_button->SetTooltipText(u"Boost this site");
-  seoul_boost_button->GetViewAccessibility().SetName(u"Boost this site");
-  seoul_boost_button->SetVisible(false);
-  seoul_boost_button_ = AddChildView(std::move(seoul_boost_button));
-
-  // The Handset control sits beside the paintbrush because it answers the
-  // neighbouring question about the same page: not "restyle this site" but
-  // "show me this site the way a phone gets it". Putting it anywhere else -
-  // a menu, the command surface - makes it undiscoverable in the same way the
-  // Boost editor was before it had a button here.
-  auto seoul_handset_button = std::make_unique<views::ImageButton>(
-      base::BindRepeating(&LocationBarView::OnSeoulHandsetButtonPressed,
-                          base::Unretained(this)));
-  seoul_handset_button->SetImageHorizontalAlignment(
-      views::ImageButton::ALIGN_CENTER);
-  seoul_handset_button->SetImageVerticalAlignment(
-      views::ImageButton::ALIGN_MIDDLE);
-  seoul_handset_button->SetTooltipText(u"Show this site as a phone");
-  seoul_handset_button->GetViewAccessibility().SetName(
-      u"Show this site as a phone");
-  seoul_handset_button->SetVisible(false);
-  seoul_handset_button_ = AddChildView(std::move(seoul_handset_button));
-
-  // Shields completes the site-control row: restyle it (Boost), reshape it
-  // (Handset), and decide what it may load (Shields). Brave puts this switch
-  // on the same edge of its address bar for the same reason - what the
-  // blocker does to a page is a per-site decision, made where the site is
-  // named.
-  auto seoul_shields_button = std::make_unique<views::ImageButton>(
-      base::BindRepeating(&LocationBarView::OnSeoulShieldsButtonPressed,
-                          base::Unretained(this)));
-  seoul_shields_button->SetImageHorizontalAlignment(
-      views::ImageButton::ALIGN_CENTER);
-  seoul_shields_button->SetImageVerticalAlignment(
-      views::ImageButton::ALIGN_MIDDLE);
-  seoul_shields_button->SetTooltipText(u"Shields for this site");
-  seoul_shields_button->GetViewAccessibility().SetName(
-      u"Shields for this site");
-  seoul_shields_button->SetVisible(false);
-  seoul_shields_button_ = AddChildView(std::move(seoul_shields_button));
+  controls->SetImageHorizontalAlignment(views::ImageButton::ALIGN_CENTER);
+  controls->SetImageVerticalAlignment(views::ImageButton::ALIGN_MIDDLE);
+  controls->SetTooltipText(u"Site controls");
+  controls->GetViewAccessibility().SetName(u"Site controls");
+  controls->GetViewAccessibility().SetHasPopup(ax::mojom::HasPopup::kMenu);
+  controls->GetViewAccessibility().SetIsCollapsed();
+  controls->SetVisible(false);
+  seoul_site_controls_button_ = AddChildView(std::move(controls));
 
   if (page_info::IsMerchantTrustFeatureEnabled() &&
       page_info::kMerchantTrustEnableOmniboxChip.Get()) {
@@ -1034,6 +993,15 @@
   // label/chip.
   const double kLeadingDecorationMaxFraction = 0.5;
 
+  // The native Site controls menu includes page information. Avoid a second
+  // tuning icon on ordinary secure pages; leave warning/identity labels and
+  // permission indicators in their existing prominent positions.
+  const bool page_info_in_site_controls =
+      GetWebContents() &&
+      GetWebContents()->GetLastCommittedURL().SchemeIsHTTPOrHTTPS() &&
+      GetLocationBarModel()->GetSecurityLevel() == security_state::SECURE &&
+      !location_icon_view_->GetShowText() && !IsEditingOrEmpty();
+
   const bool show_overriding_permission_chip =
       !seoul_floating_mode_ &&
       (base::FeatureList::IsEnabled(
@@ -1094,7 +1062,8 @@
     seoul_action_mode_label_->SetVisible(false);
     const bool show_search_icon = IsEditingOrEmpty();
     const bool show_identity_icon =
-        seoul_sidebar_chrome_revealed_ && !show_search_icon;
+        seoul_sidebar_chrome_revealed_ && !show_search_icon &&
+        !page_info_in_site_controls;
     location_icon_view_->SetVisible(show_identity_icon);
     seoul_floating_search_icon_->SetVisible(show_search_icon);
     if (show_search_icon) {
@@ -1135,6 +1104,10 @@
       }
       selected_keyword_view_->SetCustomImage(image);
     }
+  } else if (page_info_in_site_controls) {
+    seoul_floating_search_icon_->SetVisible(false);
+    seoul_action_mode_label_->SetVisible(false);
+    location_icon_view_->SetVisible(false);
   } else if (location_icon_view_->GetShowText() &&
              !ShouldChipOverrideLocationIcon()) {
     seoul_floating_search_icon_->SetVisible(false);
@@ -1193,74 +1166,21 @@
     }
   };
 
-  // Arc's paintbrush. It belongs to the resting address field: while the field
-  // is floating the user is editing a query, not looking at a page, and there
-  // is no site to restyle. It is also hidden for anything that is not a real
-  // http(s) page, because a Boost is scoped to a web origin.
-  if (seoul_boost_button_) {
+  if (seoul_site_controls_button_) {
     content::WebContents* const contents = GetWebContents();
     const GURL page = contents ? contents->GetLastCommittedURL() : GURL();
-    const bool boostable = !seoul_floating_mode_ && page.SchemeIsHTTPOrHTTPS();
-    seoul_boost_button_->SetVisible(boostable);
-    if (boostable) {
-      seoul_boost_button_->SetImageModel(
+    const bool visible = !seoul_floating_mode_ && page.SchemeIsHTTPOrHTTPS();
+    seoul_site_controls_button_->SetVisible(visible);
+    if (visible) {
+      seoul_site_controls_button_->SetImageModel(
           views::Button::STATE_NORMAL,
           ui::ImageModel::FromVectorIcon(
-              kSeoulBoostIcon, kColorOmniboxText,
+              kSeoulSiteControlsIcon,
+              seoul::IsHandsetModeEnabled(contents)
+                  ? kColorOmniboxResultsIconSelected : kColorOmniboxText,
               GetLayoutConstant(LayoutConstant::kLocationBarTrailingIconSize)));
-      add_trailing_decoration(seoul_boost_button_, /*intra_item_padding=*/0,
-                              /*edge_padding=*/icon_left);
-    }
-  }
-
-  // Same visibility rules as the paintbrush, and for the same reasons: while
-  // the field floats the user is editing a query rather than looking at a
-  // page, and a device presentation is scoped to a real web page. Eligibility
-  // itself is Seoul's answer, not this view's, so the button and every other
-  // entry point cannot disagree about what can be presented.
-  if (seoul_handset_button_) {
-    content::WebContents* const contents = GetWebContents();
-    const bool presentable =
-        !seoul_floating_mode_ && seoul::CanHandsetWebContents(contents);
-    seoul_handset_button_->SetVisible(presentable);
-    if (presentable) {
-      // The control reports the live state rather than only the action, so a
-      // page already presented as a phone is legible at a glance instead of
-      // leaving the user to infer it from the layout.
-      const bool active = seoul::IsHandsetModeEnabled(contents);
-      seoul_handset_button_->SetImageModel(
-          views::Button::STATE_NORMAL,
-          ui::ImageModel::FromVectorIcon(
-              kSeoulHandsetIcon,
-              active ? kColorOmniboxResultsIconSelected : kColorOmniboxText,
-              GetLayoutConstant(LayoutConstant::kLocationBarTrailingIconSize)));
-      // "Stop showing..." would be a lie now that the click opens a picker
-      // rather than toggling directly - turning it off is one item in that
-      // menu, not the click itself.
-      seoul_handset_button_->SetTooltipText(
-          active ? u"Change or stop the phone presentation"
-                 : u"Show this site as a phone");
-      add_trailing_decoration(seoul_handset_button_, /*intra_item_padding=*/0,
-                              /*edge_padding=*/icon_left);
-    }
-  }
-
-  // Shields shares the paintbrush's visibility rule: a real http(s) page,
-  // not a floating query - the blocker's per-site decisions are scoped to a
-  // web origin exactly as a Boost is.
-  if (seoul_shields_button_) {
-    content::WebContents* const contents = GetWebContents();
-    const GURL page = contents ? contents->GetLastCommittedURL() : GURL();
-    const bool shieldable =
-        !seoul_floating_mode_ && page.SchemeIsHTTPOrHTTPS();
-    seoul_shields_button_->SetVisible(shieldable);
-    if (shieldable) {
-      seoul_shields_button_->SetImageModel(
-          views::Button::STATE_NORMAL,
-          ui::ImageModel::FromVectorIcon(
-              vector_icons::kShieldIcon, kColorOmniboxText,
-              GetLayoutConstant(LayoutConstant::kLocationBarTrailingIconSize)));
-      add_trailing_decoration(seoul_shields_button_, /*intra_item_padding=*/0,
+      add_trailing_decoration(seoul_site_controls_button_,
+                              /*intra_item_padding=*/0,
                               /*edge_padding=*/icon_left);
     }
   }
@@ -2597,30 +2517,9 @@
   }
 }
 
-void LocationBarView::OnSeoulBoostButtonPressed() {
-  if (content::WebContents* const contents = GetWebContents()) {
-    seoul::OpenBoostEditorForWebContents(contents);
-  }
-}
-
-void LocationBarView::OnSeoulShieldsButtonPressed() {
-  if (content::WebContents* const contents = GetWebContents()) {
-    seoul::ShowShieldsBubbleForWebContents(contents);
-  }
-}
-
-void LocationBarView::OnSeoulHandsetButtonPressed() {
-  content::WebContents* const contents = GetWebContents();
-  if (!contents || !seoul_handset_button_) {
-    return;
-  }
-  // A device lab needs a picker as the primary interaction, not a single
-  // hardcoded device behind a plain toggle - "which phone" is the question
-  // being asked here, and a bare on/off control cannot answer it. Selecting
-  // an item reloads the page under the new identity, same as the old toggle
-  // did; the control's own appearance is recomputed on the next layout pass
-  // once the menu closes, the same way the boost button's menu is.
-  seoul_handset_picker_menu_.Show(seoul_handset_button_, contents);
+void LocationBarView::OnSeoulSiteControlsPressed(const ui::Event& event) {
+  seoul_site_controls_menu_.Show(seoul_site_controls_button_, GetWebContents(),
+                                  event);
 }
 
 void LocationBarView::SetSeoulActionModeLabelVisible(bool visible) {
diff --git a/chrome/browser/ui/views/location_bar/location_bar_view.h b/chrome/browser/ui/views/location_bar/location_bar_view.h
--- a/chrome/browser/ui/views/location_bar/location_bar_view.h
+++ b/chrome/browser/ui/views/location_bar/location_bar_view.h
@@ -19,7 +19,7 @@
 #include "base/scoped_observation.h"
 #include "build/build_config.h"
 #include "chrome/browser/ui/location_bar/location_bar.h"
-#include "seoul/browser/product/browser/handset_picker_menu.h"  // nogncheck
+#include "seoul/browser/product/browser/seoul_site_controls_menu.h"  // nogncheck
 #include "chrome/browser/ui/omnibox/omnibox_popup_state_manager.h"
 #include "chrome/browser/ui/page_action/page_action_icon_type.h"
 #include "chrome/browser/ui/views/location_bar/content_setting_image_view.h"
@@ -309,14 +309,8 @@
   // Shows or hides the "Actions" chip without leaving action mode, for a
   // query that named no command and is being handed back to the omnibox.
   void SetSeoulActionModeLabelVisible(bool visible);
-  views::ImageButton* seoul_boost_button_for_testing() {
-    return seoul_boost_button_;
-  }
-  views::ImageButton* seoul_handset_button_for_testing() {
-    return seoul_handset_button_;
-  }
-  views::ImageButton* seoul_shields_button_for_testing() {
-    return seoul_shields_button_;
+  views::ImageButton* seoul_site_controls_button_for_testing() {
+    return seoul_site_controls_button_;
   }
   // Expands the docked Single Toolbar field into the floating command surface
   // on the keystroke that begins editing, before that keystroke reaches the
@@ -397,9 +391,7 @@
   void RefreshSeoulFloatingSearchIcon();
   void RefreshSeoulFloatingFont();
   void UpdateSeoulSidebarChromeOpacity();
-  void OnSeoulBoostButtonPressed();
-  void OnSeoulHandsetButtonPressed();
-  void OnSeoulShieldsButtonPressed();
+  void OnSeoulSiteControlsPressed(const ui::Event& event);
   void HideSeoulSidebarChromeAfterFade();
 
   // Updates the visibility state of the Content Blocked icons to reflect what
@@ -628,17 +620,8 @@
   // updates cannot turn it back into Chromium's interactive identity pill.
   raw_ptr<views::ImageView> seoul_floating_search_icon_ = nullptr;
   raw_ptr<views::Label> seoul_action_mode_label_ = nullptr;
-  // Arc's Site Control Panel paintbrush: the discoverable entry point to the
-  // Boost editor for the page in the address field.
-  raw_ptr<views::ImageButton> seoul_boost_button_ = nullptr;
-  // The Handset control, beside it: presents the page in the address field as
-  // a phone. Same panel, same rules, one action apart.
-  raw_ptr<views::ImageButton> seoul_handset_button_ = nullptr;
-  raw_ptr<views::ImageButton> seoul_shields_button_ = nullptr;
-  // Owns the device-picker menu's model and runner across the
-  // asynchronous show/click round trip. See handset_picker_menu.h for why
-  // this cannot be a call-scoped local.
-  seoul::HandsetPickerMenu seoul_handset_picker_menu_;
+  raw_ptr<views::ImageButton> seoul_site_controls_button_ = nullptr;
+  seoul::SiteControlsMenu seoul_site_controls_menu_;
 
   // Views to show inline autocompletion when an IME is active.  In this case,
   // we shouldn't change the text or selection inside the OmniboxView itself,
diff --git a/chrome/app/vector_icons/BUILD.gn b/chrome/app/vector_icons/BUILD.gn
--- a/chrome/app/vector_icons/BUILD.gn
+++ b/chrome/app/vector_icons/BUILD.gn
@@ -182,6 +182,7 @@
     "seoul_back.icon",
     "seoul_download.icon",
     "seoul_boost.icon",
+    "seoul_site_controls.icon",
     "seoul_collapse_sidebar.icon",
   "seoul_expand_sidebar.icon",
     "seoul_forward.icon",
diff --git a/chrome/app/vector_icons/seoul_site_controls.icon b/chrome/app/vector_icons/seoul_site_controls.icon
new file mode 100644
--- /dev/null
+++ b/chrome/app/vector_icons/seoul_site_controls.icon
@@ -0,0 +1,16 @@
+// Copyright 2026 The Project Seoul Authors
+// Use of this source code is governed by a BSD-style license that can be
+// found in the LICENSE file.
+
+CANVAS_DIMENSIONS, 18,
+STROKE, 1.5,
+MOVE_TO, 2, 5,
+LINE_TO, 5, 5,
+MOVE_TO, 9, 5,
+LINE_TO, 16, 5,
+MOVE_TO, 2, 13,
+LINE_TO, 9, 13,
+MOVE_TO, 13, 13,
+LINE_TO, 16, 13,
+CIRCLE, 7, 5, 2,
+CIRCLE, 11, 13, 2,
