--- a/chrome/browser/ui/color/chrome_color_provider_utils.h
+++ b/chrome/browser/ui/color/chrome_color_provider_utils.h
@@ -14,8 +14,10 @@
 #include "ui/gfx/color_utils.h"
 
 inline constexpr SkAlpha kToolbarInkDropHighlightVisibleAlpha = 0x14;
-inline constexpr SkAlpha kBrowserFrameAlphaLight = 95;
-inline constexpr SkAlpha kBrowserFrameAlphaDark = 180;
+// Keep the sky surface recognizable over macOS sidebar material. A small
+// amount of translucency retains depth without washing the product tint gray.
+inline constexpr SkAlpha kBrowserFrameAlphaLight = 218;
+inline constexpr SkAlpha kBrowserFrameAlphaDark = 230;
 
 // Converts ColorId if |color_id| is in CHROME_COLOR_IDS.
 std::string ChromeColorIdName(ui::ColorId color_id);
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_bottom_container.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_bottom_container.cc
@@ -115,6 +115,7 @@
 }
 
 void VerticalTabStripBottomContainer::UpdateButtonStyles(bool collapsed) {
+  new_tab_button_->SetShouldShowLabel(!collapsed);
   auto orientation = collapsed ? views::LayoutOrientation::kVertical
                                : views::LayoutOrientation::kHorizontal;
 
--- a/chrome/browser/ui/views/tabs/shared/tab_strip_flat_edge_button.cc
+++ b/chrome/browser/ui/views/tabs/shared/tab_strip_flat_edge_button.cc
@@ -169,6 +169,7 @@
     label()->layer()->SetFillsBoundsOpaquely(false);
     label()->SetSubpixelRenderingEnabled(false);
   }
+  UpdateLabel(width() > CalculatePreferredSize({}).width());
 }
 
 void TabStripFlatEdgeButton::SetPaintBackground(bool paint_background) {
@@ -321,9 +322,7 @@
 }
 
 void TabStripFlatEdgeButton::UpdateLabel(bool should_show) {
-  if (!should_show_label_) {
-    return;
-  }
+  should_show = should_show && should_show_label_;
   if (should_show == GetText().empty()) {
     SetHorizontalAlignment(should_show ? gfx::ALIGN_LEFT : gfx::ALIGN_CENTER);
     SetInsets(should_show ? gfx::Insets::VH(0, kButtonWithLabelPadding)
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_view.cc
@@ -9,6 +9,7 @@
 
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
+#include "base/i18n/rtl.h"
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/user_metrics.h"
 #include "base/metrics/user_metrics_action.h"
@@ -64,6 +65,8 @@
 #include "ui/base/models/list_selection_model.h"
 #include "ui/base/theme_provider.h"
 #include "ui/base/ui_base_features.h"
+#include "ui/color/color_id.h"
+#include "ui/color/color_provider.h"
 #include "ui/color/color_provider_key.h"
 #include "ui/compositor/layer.h"
 #include "ui/compositor/layer_animator.h"
@@ -766,6 +769,19 @@
                           flags);
   }
 
+  // Icon-only tabs need a persistent shape cue; nearly identical pale fills
+  // are not sufficient to identify the active page without its title.
+  if (collapsed_ && !IsInExpandOnHover(width()) &&
+      selection_state == TabStyle::TabSelectionState::kActive) {
+    cc::PaintFlags indicator;
+    indicator.setAntiAlias(true);
+    indicator.setColor(GetColorProvider()->GetColor(
+        ui::kColorAccent));
+    const float x = base::i18n::IsRTL() ? width() - 4.0f : 1.0f;
+    canvas->DrawRoundRect(gfx::RectF(x, (height() - 16) / 2.0f, 3, 16),
+                          1.5f, indicator);
+  }
+
   if (fill_id.has_value()) {
     gfx::ImageSkia* image =
         GetThemeProvider()->GetImageSkiaNamed(fill_id.value());
