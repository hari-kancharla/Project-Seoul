diff --git a/chrome/browser/ui/chrome_pages.cc b/chrome/browser/ui/chrome_pages.cc
--- a/chrome/browser/ui/chrome_pages.cc
+++ b/chrome/browser/ui/chrome_pages.cc
@@ -3,6 +3,8 @@
 // found in the LICENSE file.
 
 #include "chrome/browser/ui/chrome_pages.h"
+
+#include "seoul/browser/product/browser/seoul_settings_window.h"  // nogncheck
 
 #include <stddef.h>
 
@@ -454,6 +456,11 @@
 }
 
 void ShowSettings(BrowserWindowInterface* browser) {
+#if BUILDFLAG(IS_MAC)
+  if (seoul::ShowSeoulSettings(browser->GetProfile())) {
+    return;
+  }
+#endif
   ShowSettingsSubPage(browser, std::string());
 }
 
diff --git a/chrome/browser/profiles/keep_alive/profile_keep_alive_types.h b/chrome/browser/profiles/keep_alive/profile_keep_alive_types.h
--- a/chrome/browser/profiles/keep_alive/profile_keep_alive_types.h
+++ b/chrome/browser/profiles/keep_alive/profile_keep_alive_types.h
@@ -183,7 +183,10 @@
   // Used during WebApp database cleanup from corruption
   kWebAppDatabaseCorruptionRecovery = 44,
 
-  kMaxValue = kWebAppDatabaseCorruptionRecovery,
+  // The native Seoul Settings window is editing this profile.
+  kSeoulSettingsWindow = 45,
+
+  kMaxValue = kSeoulSettingsWindow,
 };
 // LINT.ThenChange(/tools/metrics/histograms/metadata/profile/enums.xml)
 
diff --git a/chrome/browser/profiles/keep_alive/profile_keep_alive_types.cc b/chrome/browser/profiles/keep_alive/profile_keep_alive_types.cc
--- a/chrome/browser/profiles/keep_alive/profile_keep_alive_types.cc
+++ b/chrome/browser/profiles/keep_alive/profile_keep_alive_types.cc
@@ -89,6 +89,8 @@
       return out << "kSkillsDialog";
     case ProfileKeepAliveOrigin::kWebAppDatabaseCorruptionRecovery:
       return out << "kWebAppDatabaseCorruptionRecovery";
+    case ProfileKeepAliveOrigin::kSeoulSettingsWindow:
+      return out << "kSeoulSettingsWindow";
   }
   NOTREACHED();
 }
diff --git a/tools/metrics/histograms/metadata/profile/enums.xml b/tools/metrics/histograms/metadata/profile/enums.xml
--- a/tools/metrics/histograms/metadata/profile/enums.xml
+++ b/tools/metrics/histograms/metadata/profile/enums.xml
@@ -384,6 +384,7 @@
   <int value="42" label="kWaitingForBookmarksImportOnFirstRun"/>
   <int value="43" label="kSkillsDialog"/>
   <int value="44" label="kWebAppDatabaseCorruptionRecovery"/>
+  <int value="45" label="kSeoulSettingsWindow"/>
 </enum>
 
 <!-- LINT.IfChange(ProfileManagementFlowStep) -->
